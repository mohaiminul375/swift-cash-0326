1) What is the difference between null and undefined?
null means explicitly setting a value to null, which is a developer’s choice, whereas undefined indicates that a value is missing or that the query returned an incorrect or non-existent value(it's a mistake).

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
=> map loop a collection of array of object and return each object. and forEch also do same but not return anything.

3) What is the difference between == and ===?
=> Difference between == and === is, == compare between only value whereas === compare between value and type both.
And == use type coercion it change both side type like true to false and false to true .

4) What is the significance of async/await in fetching API data?
=>

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
=> Global Scope: Variables declared outside any function or block. Accessible from anywhere.

Function: Variables declared inside a function. Accessible only inside that function.

Block Scope: Only let and const declared and accessible inside specific block not other block or outside. But var one kind of global access it can accessible from outside